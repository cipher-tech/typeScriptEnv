const missingInteger = (array: number[]) => {
    const map: any = {};
    let min = 1;

    for (let i = 0; i < array.length; i++) {
        const element = array[i];
    }
    if(!map[min]) return 1

    while(map[min]) min++

    return min
}

export default missingInteger;